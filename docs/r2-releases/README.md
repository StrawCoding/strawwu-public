# R2 release markers

Each `vX.Y.Z.W.json` marks a whole-file ISO published to Cloudflare R2
(`strawwu-releases`). `generate-manifest.sh` prefers these over repo/LFS parts
or SourceForge markers.

Created automatically by `scripts/publish-github-release.sh` after a successful
S3/R2 upload.
